# Dependencies for deploy/llm_proxy.py only. This runs on YOUR machine next to
# LM Studio, never on Streamlit Cloud, which is why it is kept out of the
# top-level requirements.txt that Cloud installs.
#
# All three are already present in the app venv (starlette is pinned by the app,
# uvicorn and httpx arrive with streamlit and the OpenAI SDK). They are pinned
# again here so the proxy can be run from a bare environment if needed.

starlette==1.3.1
uvicorn==0.51.0
httpx==0.28.1
